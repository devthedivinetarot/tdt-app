// Bundled ginni-kb topic files. This file is regenerated by
// `node scripts/fetch-kb.mjs`, which downloads all 13 topic files from the repo
// for 100% offline readings. Until then, these 3 are bundled and the rest are
// fetched + cached at runtime by the reading engine.
export default {
  universe_guidance: require('./universe_guidance.json'),
  yes_no: require('./yes_no.json'),
  connection: require('./connection.json'),
};
